export enum MatchStatus {
  Scheduled = 0,
  Live = 1,
  Finished = 2,
}

export enum PlayerPosition {
  MB = 'MB', // Middle-blocker
  O = 'O', // Opposite
  WS = 'WS', // Wing-spiker
  S = 'S', // Setter
  L = 'L', // Libero
  U = 'U', // Universal
}
